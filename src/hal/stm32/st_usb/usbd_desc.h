/**
 * usbd_desc.h — minimal placeholder.
 *
 * usbd_core.c / usbd_req.c #include "usbd_desc.h" in the ST distribution to
 * pull in the example's descriptor buffers. In this project the descriptor
 * callbacks are supplied through the USBD_DEVICE structure (see usb.c), so no
 * example-specific descriptor symbols are needed. This empty header satisfies
 * the include without dragging unrelated MSC/example code into the build.
 */
#ifndef __USBD_DESC_H__
#define __USBD_DESC_H__

/* intentionally empty: descriptors are provided via USBD_DEVICE in usb.c */

#endif /* __USBD_DESC_H__ */
